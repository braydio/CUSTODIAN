# Prop Definitions

Store `PropDefinition` `.tres` resources here. Each definition should bind a padded base texture, optional variant layers or legacy overlay arrays, rubble options, spawn rectangles, and an optional stable collision scene.
