# Prop Definitions

Store `PropDefinition` `.tres` resources here. Each definition should bind a padded base texture, optional variant layers or legacy overlay arrays, rubble options, spawn rectangles, and either an optional stable collision scene or a simple authored collision footprint plus optional player-relative depth sorting for tall props.
