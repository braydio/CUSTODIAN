# Extracted Ruin Props

Store transparent, padded, per-prop PNGs here. Runtime `PropDefinition.base_texture` resources should point at these files.

The initial padded copies in this folder were generated from the flat PNGs in `content/props/ruins/` with 16 px transparent padding on every side.
