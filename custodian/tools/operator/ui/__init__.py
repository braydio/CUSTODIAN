"""Optional interactive client for the Operator Animation Workbench."""

OPTIONAL_DEPENDENCY_MESSAGE = (
    "operator ui requires the optional Textual dependency; install it in a "
    "virtual environment with: python3 -m pip install -r "
    "custodian/tools/operator/ui/requirements.txt"
)
