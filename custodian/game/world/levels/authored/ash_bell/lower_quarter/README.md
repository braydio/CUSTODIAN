# Lower Quarter blockout

First-pass procedural authored blockout for the `ash_bell_lower_quarter` route.
The local grid uses 32-world-unit authored cells; it is not global tile-scale
authority. Route destinations remain owned by `RouteTraversalManager`.

Production art, final dialogue, and Penitent population are deferred until the
spatial/FOV blockout is reviewed in engine.
