# PARRY CRITICAL BRANCHING AND VFX

This repository-root packet is retained only as a routing stub.

The active Godot implementation authority is:

`design/02_features/combat_feel/PARRY_CRITICAL_BRANCHING_AND_VFX.md`

For runtime work, also follow `custodian/AGENTS.md` and the current state under `custodian/docs/ai_context/`. Historical implementation claims formerly duplicated here are intentionally removed so they cannot conflict with the active design.
